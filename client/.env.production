# API Configuration
REACT_APP_API_URL=https://aiml-forum.onrender.com

# Other client-side environment variables (if needed)
REACT_APP_GOOGLE_ANALYTICS_ID=your_ga_id_here

# Canonical site URL, used for the static og:url social preview tag
# (client/index.html). This is the Netlify site's default domain
# (confirmed via this repo's own deploy-preview bot comments); update
# here or override in the deploy environment (e.g. Netlify's env var UI)
# if a custom domain is ever attached.
REACT_APP_SITE_URL=https://cerulean-marshmallow-003d16.netlify.app
